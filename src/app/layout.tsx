import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import { Providers } from '@/components/Providers'
import './globals.css'

const inter = Inter({ 
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Rivora DeFi Landing Page - Premium Digital Masterpiece',
  description: 'Step into the future of decentralized finance with Rivora - where security meets innovation.',
  keywords: ['DeFi', 'Decentralized Finance', 'Crypto', 'Blockchain', 'Web3'],
  authors: [{ name: 'Rivora Team' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}